name: Github Actions

on:
  push:
     branches: [main]
     
jobs:
   echo-jobs:
     runs-on: ubuntu-latest

     steps:

       - name: just print github 
         run: echo "Hello, this is my second GitHub Action!"
       - name: Current WD
         run: echo "Current working directory:$(pwd)"
       - name: List of files in wd
         run: echo "List of files in the repo$(ls -la)"
         
   
